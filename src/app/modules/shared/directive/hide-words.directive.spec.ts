import { HideWordsDirective } from './hide-words.directive';

describe('HideWordsDirective', () => {
  it('should create an instance', () => {
    const directive = new HideWordsDirective();
    expect(directive).toBeTruthy();
  });
});
