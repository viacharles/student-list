import { TouchMoveDirective } from './touch-move.directive';

describe('TouchMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchMoveDirective();
    expect(directive).toBeTruthy();
  });
});
